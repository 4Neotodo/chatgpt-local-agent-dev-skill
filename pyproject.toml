[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "chatgpt-local-agent-dev-skill"
version = "0.1.0"
description = "Generate a disciplined ChatGPT and local-Agent project governance baseline."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "4Neotodo" }]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
project-governance-init = "project_governance_skill.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
project_governance_skill = ["templates/*.tpl"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
